# API Configuration
# Thay đổi IP address này thành IP của máy tính bạn
# Để kiểm tra IP, chạy lệnh: ipconfig (Windows) hoặc ifconfig (Mac/Linux)
# Tạo file .env từ file này và chỉnh sửa giá trị bên dưới (Chỉ thay đổi phần IP, giữ nguyên cổng 5001 nếu không có thay đổi gì khác)
API_URL=http://192.168.1.62:5001
